<div class="container">
  <br />
  <br />
  <h1> Only Fams! </h1>

  <% if current_user.nil? %>
    <%= link_to 'Sign in', new_user_session_path %>
    <br />
    <br />
  <% else %>
    <%= link_to User.find(current_user.id).name, "/user/#{current_user.id}" %>
    <br />
    <%= link_to 'Sign out', destroy_user_session_path %>
  <% end %>

  <%= link_to 'Post', 'posts' %>
</div>
