      <br />Comments:
        <% post.comments.each do |comment| %>
          <br />
          <strong><%= User.find(comment.user_id).name %></strong>
          <br />
          <%= comment.body %>
        <% end %>
